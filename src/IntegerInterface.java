public interface IntegerInterface {
    IntegerInterface add(IntegerInterface other);
    IntegerInterface subtract(IntegerInterface other);
    IntegerInterface multiply(IntegerInterface other);
    boolean equals(Object obj);
}
